services:
  - type: web
    name: lingban
    env: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile && npx prisma generate && pnpm run build
    startCommand: npx prisma migrate deploy && pnpm run start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: file:./prisma/dev.db
