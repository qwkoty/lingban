# 数据库 URL（PostgreSQL）
# 本地开发：可连接本地 PostgreSQL 或 Docker 启动的 Postgres
# 示例：postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/lingban?schema=public"

# 运行环境
NODE_ENV=development

# 后端服务端口
PORT=3001
